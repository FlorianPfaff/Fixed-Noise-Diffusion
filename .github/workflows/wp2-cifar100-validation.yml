name: WP2 CIFAR-100 validation superseded

on:
  workflow_dispatch:

jobs:
  superseded:
    runs-on: ubuntu-latest
    steps:
      - name: Stop
        shell: bash
        run: |
          echo "This workflow has been superseded. Use WP2 STL-10 validation instead."
          exit 1
